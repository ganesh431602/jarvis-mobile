export const DashboardHeader = ({ onRefresh, loading }: { onRefresh: () => void; loading: boolean }): JSX.Element => <header className="dashboard-heading"><div><p className="eyebrow">Overview</p><h1>Command center</h1><p className="muted">A focused view of system activity and decisions that need a human.</p></div><button className="button button-secondary refresh-button" onClick={onRefresh} disabled={loading}>{loading ? "Refreshing…" : "Refresh"}</button></header>;
