export const SystemStatus = ({ status }: { status?: string }): JSX.Element => <section className="dashboard-lead surface"><div className="section-heading"><div><p className="eyebrow">System status</p><h2>{status ?? "Status unavailable"}</h2></div><span className="status-badge"><span className="status-dot status-neutral" />{status ? "Reported by backend" : "Not connected"}</span></div><p className="muted">Operational state is shown only when the backend reports it.</p></section>;
