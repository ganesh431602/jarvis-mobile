import React from "react";
export const SecuritySummary = ({ lockState }: { lockState?: string }): React.ReactElement => <section className="surface"><p className="eyebrow">Security</p><h2>{lockState ? `Lock state: ${lockState}` : "Security state unavailable"}</h2><p className="muted">Decisions remain authoritative on the backend.</p><a className="text-link" href="/security">Open Security Center</a></section>;
