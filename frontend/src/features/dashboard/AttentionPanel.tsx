export const AttentionPanel = ({ approvals, failed }: { approvals?: number; failed?: number }): JSX.Element => <section className="surface attention-panel"><p className="eyebrow">Requires attention</p>{approvals === undefined && failed === undefined ? <><h2>Attention data unavailable</h2><p className="muted">Approvals and failed operations will appear when connected.</p></> : approvals === 0 && failed === 0 ? <><h2>No items require attention</h2><p className="muted">The connected sources returned no urgent items.</p></> : <><h2>Review recommended</h2><p className="muted">{approvals ?? 0} approvals · {failed ?? 0} failed operations</p></>}</section>;
