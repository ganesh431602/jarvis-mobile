export const RecentActivity = ({ events }: { events?: readonly { timestamp: string; actor: string; action: string; result: string }[] }): JSX.Element => <section className="surface activity-panel"><div className="section-heading"><div><p className="eyebrow">Recent activity</p><h2>Movement</h2></div></div>{events === undefined ? <p className="muted">Activity unavailable.</p> : events.length === 0 ? <p className="muted">No activity yet.</p> : <ul className="activity-list">{events.map((event) => <li key={`${event.timestamp}-${event.actor}-${event.action}`}><time>{event.timestamp}</time><span><strong>{event.actor}</strong>{event.action}</span><em>{event.result}</em></li>)}</ul>}</section>;
