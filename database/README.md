# Database foundation

Supabase/PostgreSQL is the planned database platform. Phase 1 contains no SQL, migrations, seed data, project identifiers, or connection code.

All schema changes must be version-controlled migrations. Secrets never belong in ordinary JSONB metadata.
