# Dashboard module

The dashboard is read-oriented and aggregates summary metadata only. It never owns security policy, lock enforcement, agent privileges, or business execution logic. It summarizes counts and safe status information without exposing secrets.
