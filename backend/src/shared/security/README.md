# Security boundary

`security` owns cross-cutting security middleware, emergency/global/scoped locks, deterministic risk classification, and security policies. `permissions` owns capability definitions and authorization checks; neither may form a circular dependency.
