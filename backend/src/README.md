# Batch 3 backend foundation

This layer establishes the Express application boundary, request context, safe errors, configuration loading, health check, and framework-neutral authentication/authorization interfaces.

No authentication provider, database, Supabase adapter, business module, or API feature is implemented. Authorization is deny-by-default until a future security implementation is explicitly added.
