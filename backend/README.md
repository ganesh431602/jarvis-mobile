# Backend foundation

The backend will host the Node/Express application boundary. This skeleton contains no routes, controllers, business logic, authentication implementation, or provider connection.

Domain modules own cohesive capabilities. Cross-cutting security is server-side and default-deny.
